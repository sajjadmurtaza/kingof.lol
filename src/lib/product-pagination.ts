/** Default number of products per paginated list page. */
export const PRODUCT_PAGE_SIZE = 10;

/** Hard cap for API `limit` / `pageSize` query params. */
export const PRODUCT_PAGE_SIZE_MAX = 50;

/** Products listed within this window appear on /new and "new listings" sections. */
export const NEW_LISTINGS_WITHIN_MINUTES = 5;

/** How far below the viewport (px) infinite scroll may start loading the next page. */
export const PRODUCT_LIST_SCROLL_MARGIN_PX = 200;
