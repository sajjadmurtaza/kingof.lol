import TwitterImage, {
  alt as rootAlt,
  size as rootSize,
  contentType as rootContentType,
} from "../twitter-image";

export const alt = rootAlt;
export const size = rootSize;
export const contentType = rootContentType;
export const runtime = "nodejs";

export default TwitterImage;
